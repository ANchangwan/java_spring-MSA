server:
  port: 8082

spring:
  application:
    name: inventory-service

  cloud:

    function:
      definition: processInventory # Consumer ??? ??

    stream:
      bindings:
        # ?? ???(????-in-???)
        # method (bean name)
        # Cosumer(????)
        # ?? ???? ? ??? ?????? -> LB?? ??? ?? ??
        processInventory-in-0:
          destination: order-events-topic # producer ??, ??? ? ??
          group: inventory-group # Consumer? ??? ??? ?? (LB ??? ?? ??)
